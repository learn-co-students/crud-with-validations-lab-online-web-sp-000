<h1>Songs</h1>
<table style="width:100%">
  <tr>
    <th>Song Title</th>
  </tr>
  <tbody>
  <%@songs.each {|song|%>
    <tr>
        <td><%=link_to song.title, song_path(song)%></td>
    </tr>
  <%}%>
  </tbody>
</table>
