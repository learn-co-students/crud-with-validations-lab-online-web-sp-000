<table>

<tbody>
<% @songs.each do |song| %>
  <tr><%= song.title %></tr>
  <%= link_to "Delete", song, method: :delete, data: { confirm: "Do you really want to delete this song?" } %>
<% end %>
</tbody>

</table>