<table>
    <thead>
        <tr>
            <th>Title</th>
        </tr>
    </thead>
    <tbody>
        <% @songs.each do |song| %>
        <tr><%= song.title %></tr>
        <% end %>
    </tbody>
</table>