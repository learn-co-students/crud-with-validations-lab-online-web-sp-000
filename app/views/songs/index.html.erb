<tbody>
    <% @songs.each do |song| %>
        <tr>
        <li><%= song.title %><li>
        <li><%= song.released %></li>
        <li><%= song.release_year %></li>
        <li><%= song.artist_name %></li>
        <li><%= song.genre %></li>
        </tr>
    <% end %>
</tbody>
