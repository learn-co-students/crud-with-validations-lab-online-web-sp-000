<h2>Songs</h2>

    <% @songs.each do |song| %>
      <ul>
        <li><%= song.title %></li>
        <li><%= song.release_year %></li>
        <li><%= song.artist_name %></li>
        <li><%= song.genre %></li>
      </ul>

    <% end %>
