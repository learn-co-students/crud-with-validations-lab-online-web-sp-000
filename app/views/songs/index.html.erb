

<% @songs.each do |song| %>
    <tbody>
    
       <tr>  <span><%= song.title %></span>
          <%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %>
        </tr> <br>
      
    </tbody>
    <% end %>