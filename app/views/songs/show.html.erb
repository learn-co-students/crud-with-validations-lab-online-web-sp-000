<h2><%= @song.title %></h2>
by <strong><%= @song.artist_name %></strong>
<% if @song.released? %>
  <p>
    Released in
    <strong><%= @song.release_year %></strong>
  </p>
<% end %>

<p>
  <strong>Genre:</strong>
  <%= @song.genre %>
</p>

<%= link_to 'View', @song %> |
<%= link_to 'Edit', edit_song_path(@song) %> |
<%= link_to 'Delete', @song, method: :delete, data: { confirm: 'Delete?' } %>
