<h1> <%=@song.title %> </h1>
<p>
  Artist: <%= @song.artist_name %> Genre: <%= @song.genre %>
  <br>
  <div class = 'links'>
    <%= link_to 'Edit', edit_song_path(@song)%>
    <%= link_to 'Delete', @song, method: :delete, data: {confirm: 'Are you sure you want to delete this song?' } %>
  </div>
</p>