<%= button_to "Home", songs_path, method: :get %>

<h1><%= @song.title %></h1>

<p><strong>By: </strong><%= @song.artist_name %></p>
<p><strong>Genre: </strong><%= @song.genre %></p>
<p><strong>Release date: </strong><%= @song.release_year %></p>

<%= button_to "Edit", edit_song_path(@song), method: :get %>
<br>
<%= button_to "Delete", song_path(@song), method: :delete, data: { confirm: 'Are you sure?'} %>
